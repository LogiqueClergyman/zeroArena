{
  "name": "zeroarena",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix sdk/game && npm install --prefix games/connect4 && npm install --prefix games/sovereign-bluff && npm install --prefix backend && npm install --prefix contracts && npm install --prefix frontend && npm install --prefix sdk/agent",
    "test": "npm run test:games && npm test --prefix backend && npm test --prefix sdk/agent && npm test --prefix contracts && npm run build --prefix frontend",
    "test:games": "npm test --prefix sdk/game && npm test --prefix games/connect4 && npm test --prefix games/sovereign-bluff",
    "e2e:external-connect4": "node scripts/external-connect4-e2e.mjs",
    "verify:e2e": "node scripts/e2e-check.mjs"
  }
}
