{
  "name": "zeroarena",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix contracts && npm install --prefix frontend && npm install --prefix sdk/agent",
    "test": "npm test --prefix backend && npm test --prefix sdk/agent && npm test --prefix contracts && npm run build --prefix frontend",
    "verify:e2e": "node scripts/e2e-check.mjs"
  }
}
